window.MIIS_V4_DATA=window.MIIS_V4_DATA||{};window.MIIS_V4_DATA["e83c6aff6f924b92574825e3a323ec332694c84bd57297a26400a0aaef61d73f"]="#!/usr/bin/env python3\n\"\"\"Fail-closed full OXLON run and governed export wrapper.\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport hashlib\nimport json\nimport os\nimport shutil\nimport subprocess\nimport sys\nimport fcntl\nfrom datetime import datetime, timezone\nfrom pathlib import Path\n\nBASE = Path(__file__).resolve().parent\nROOT = BASE.parents[1]\nRESULTS = ROOT / \"results\" / \"oxlon\"\nLOGS = ROOT / \"logs\"\n\n\ndef _sha256(path: Path) -> str:\n    h = hashlib.sha256()\n    with path.open(\"rb\") as fh:\n        for block in iter(lambda: fh.read(1 << 20), b\"\"):\n            h.update(block)\n    return h.hexdigest()\n\n\ndef _canonical_notebook_source(path: Path) -> str:\n    \"\"\"Hash notebook cell types and source only, excluding execution output/counts.\"\"\"\n    payload = []\n    for cell in json.loads(path.read_text(encoding=\"utf-8\")).get(\"cells\", []):\n        payload.append({\"cell_type\": cell.get(\"cell_type\"), \"source\": cell.get(\"source\", [])})\n    raw = json.dumps(payload, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode(\"utf-8\")\n    return hashlib.sha256(raw).hexdigest()\n\n\ndef _input_snapshot(run_id: str) -> dict:\n    \"\"\"Capture execution inputs and archive raw pre-execution notebooks.\"\"\"\n    rel = {\n        \"raw_workbook\": BASE / \"data\" / \"Statistik data dinamika 05.06.2026 +.xlsx\",\n        \"macro_panel\": BASE / \"data\" / \"macro_annual.csv\",\n        \"external_block\": BASE / \"data\" / \"external_block_annual.csv\",\n        \"official_2025_actuals\": BASE / \"data\" / \"official_2025_actuals.csv\",\n        \"assumption_registry\": BASE / \"data\" / \"assumption_registry.csv\",\n        \"canonical_overlay\": ROOT / \"data\" / \"official_overrides.csv\",\n        \"copied_model_overlay\": BASE / \"data\" / \"official_overrides.csv\",\n        \"deflator_corrections\": ROOT / \"data\" / \"governance\" / \"deflator_corrections.csv\",\n    }\n    code = sorted(BASE.glob(\"src/**/*.py\")) + [\n        BASE / \"run_all.py\", BASE / \"run_rebuild.py\", BASE / \"normalize_outputs.py\",\n    ]\n    notebooks = {path.stem: path for path in sorted((BASE / \"notebooks\").glob(\"*.ipynb\"))}\n    archive_dir = LOGS / \"input_snapshots\" / run_id\n    archive_dir.mkdir(parents=True, exist_ok=True)\n    notebook_inputs = {}\n    for name, path in notebooks.items():\n        if not path.exists():\n            continue\n        archive = archive_dir / path.name\n        shutil.copy2(path, archive)\n        notebook_inputs[name] = {\n            \"role\": \"raw_preexec_notebook\",\n            \"path\": str(path.relative_to(ROOT)),\n            \"raw_sha256\": _sha256(path),\n            \"source_sha256\": _canonical_notebook_source(path),\n            \"archive_path\": str(archive.relative_to(ROOT)),\n            \"archive_sha256\": _sha256(archive),\n            \"bytes\": path.stat().st_size,\n        }\n    snapshot = {\n        \"captured_at_utc\": datetime.now(timezone.utc).isoformat(),\n        \"files\": {k: {\"path\": str(v.relative_to(ROOT)), \"sha256\": _sha256(v), \"bytes\": v.stat().st_size}\n                  for k, v in rel.items() if v.exists()},\n        \"code\": {str(v.relative_to(ROOT)): {\"sha256\": _sha256(v), \"bytes\": v.stat().st_size}\n                 for v in code if v.exists()},\n        \"notebook_inputs\": notebook_inputs,\n    }\n    # Freeze the governed input file that the complete DAG will consume. The\n    # live canonical file may be edited by another process after preflight.\n    overlay = rel[\"canonical_overlay\"]\n    overlay_archive = archive_dir / \"official_overrides.csv\"\n    shutil.copy2(overlay, overlay_archive)\n    snapshot[\"files\"][\"canonical_overlay_archive\"] = {\n        \"path\": str(overlay_archive.relative_to(ROOT)),\n        \"sha256\": _sha256(overlay_archive),\n        \"bytes\": overlay_archive.stat().st_size,\n        \"role\": \"frozen_overlay_consumed_by_dag\",\n    }\n    snapshot[\"governed_overlay_execution_path\"] = str(overlay_archive.relative_to(ROOT))\n    return snapshot\n\n\ndef _write_manifest(run_id: str, status: str, **extra) -> None:\n    RESULTS.mkdir(parents=True, exist_ok=True)\n    payload = {\n        \"run_id\": run_id,\n        \"status\": status,\n        \"model_id\": \"oxlon.corrected.20260917\",\n        \"started_at_utc\": extra.pop(\"started_at_utc\"),\n        \"finished_at_utc\": (None if status == \"RUNNING\" else datetime.now(timezone.utc).isoformat()),\n        \"input_data\": {\n            \"raw_workbook\": \"models/oxlon/data/Statistik data dinamika 05.06.2026 +.xlsx\",\n            \"raw_workbook_sha256\": _sha256(BASE / \"data\" / \"Statistik data dinamika 05.06.2026 +.xlsx\"),\n            \"macro_panel\": \"models/oxlon/data/macro_annual.csv\",\n            \"deflator_correction\": \"data/governance/deflator_corrections.csv\",\n        },\n        **extra,\n    }\n    (RESULTS / \"run_manifest.json\").write_text(json.dumps(payload, ensure_ascii=False, indent=2) + \"\\n\", encoding=\"utf-8\")\n\n\ndef main() -> int:\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--kernel\", default=os.environ.get(\"MIIS_KERNEL\", \"python3.13.9\"))\n    args = ap.parse_args()\n    start = datetime.now(timezone.utc).isoformat()\n    run_id = datetime.now(timezone.utc).strftime(\"%Y%m%dT%H%M%SZ\")\n    log_path = LOGS / f\"oxlon_{run_id}.log\"\n    LOGS.mkdir(parents=True, exist_ok=True)\n    RESULTS.mkdir(parents=True, exist_ok=True)\n    lock_path = RESULTS / \".oxlon_run.lock\"\n    lock_fh = lock_path.open(\"a+\")\n    try:\n        fcntl.flock(lock_fh.fileno(), fcntl.LOCK_EX | fcntl.LOCK_NB)\n    except BlockingIOError:\n        lock_fh.close()\n        return 3\n    # Generated model outputs/cache are disposable.  Official overlays live in\n    # data/official_overrides.csv and are deliberately outside this cleanup.\n    # Remove every published derivative before RUNNING so a failed run cannot\n    # leave a stale headline beside a FAILED manifest.\n    for name in [\"forecast_normalized.csv\", \"forecast_normalized.json\",\n                 \"normalized_contract.json\", \"headline.csv\", \"headline.json\",\n                 \"series_coverage.csv\"]:\n        p = RESULTS / name\n        if p.exists(): p.unlink()\n    for name in [\"forecast_long.csv\", \"equations_catalog.csv\", \"validation_backtest.csv\",\n                 \"series_dictionary.csv\", \"neticeler_15_5_1.xlsx\",\n                 \"neticeler_15_5_1.json\"]:\n        p = BASE / \"outputs\" / name\n        if p.exists(): p.unlink()\n    # Assumptions are regenerated by the DAG; remove the prior snapshot so a\n    # failed or test-only run cannot leak an overlay row into the next run.\n    assumptions_snapshot = BASE / \"data\" / \"assumptions.csv\"\n    if assumptions_snapshot.exists(): assumptions_snapshot.unlink()\n    cache = BASE / \"data\" / \"cache\"\n    if cache.exists(): shutil.rmtree(cache)\n    cache.mkdir(parents=True, exist_ok=True)\n    # Capture this before invoking run_all.py; later SUCCESS metadata adds output hashes.\n    input_snapshot = _input_snapshot(run_id)\n    _write_manifest(run_id, \"RUNNING\", started_at_utc=start, kernel=args.kernel,\n                    input_snapshot=input_snapshot)\n    # Reject malformed governed input records before the DAG opens a notebook.\n    # The validator accepts the empty header-only file used for clean baselines.\n    try:\n        from src.governed_inputs import read_validated_overlay\n        frozen_overlay = ROOT / input_snapshot[\"governed_overlay_execution_path\"]\n        read_validated_overlay(frozen_overlay)\n    except Exception as exc:\n        _write_manifest(run_id, \"FAILED\", started_at_utc=start, kernel=args.kernel,\n                        error=f\"governed input validation failed: {exc}\",\n                        input_snapshot=input_snapshot)\n        return 2\n    env = os.environ.copy()\n    env[\"MIIS_KERNEL\"] = args.kernel\n    env[\"MIIS_GOVERNED_OVERLAY\"] = str(ROOT / input_snapshot[\"governed_overlay_execution_path\"])\n    cmd = [sys.executable, \"run_all.py\"]\n    with log_path.open(\"w\", encoding=\"utf-8\") as log:\n        proc = subprocess.run(cmd, cwd=BASE, env=env, stdout=log, stderr=subprocess.STDOUT)\n    if proc.returncode != 0:\n        # A failed DAG never leaves a publishable normalized snapshot.\n        for p in [RESULTS / \"forecast_normalized.csv\", RESULTS / \"forecast_normalized.json\",\n                  RESULTS / \"normalized_contract.json\", RESULTS / \"headline.csv\",\n                  RESULTS / \"headline.json\", RESULTS / \"series_coverage.csv\"]:\n            if p.exists(): p.unlink()\n        _write_manifest(run_id, \"FAILED\", started_at_utc=start, kernel=args.kernel,\n                        log_file=str(log_path.relative_to(ROOT)), return_code=proc.returncode,\n                        input_snapshot=input_snapshot)\n        return proc.returncode\n    try:\n        # Call the normalizer as a subprocess so the same interpreter and\n        # explicit run id are recorded in the log.\n        norm = subprocess.run([sys.executable, \"normalize_outputs.py\", \"--run-id\", run_id],\n                              cwd=BASE, env=env, capture_output=True, text=True)\n        if norm.returncode != 0:\n            raise RuntimeError(norm.stderr or norm.stdout)\n        required = {f\"FR{i}\" for i in range(1, 13)}\n        got = set(__import__(\"pandas\").read_csv(BASE / \"outputs\" / \"forecast_long.csv\")[\"fr\"])\n        missing = sorted(required - got)\n        if missing: raise RuntimeError(f\"missing FR outputs: {missing}\")\n    except Exception as exc:\n        for p in [RESULTS / \"forecast_normalized.csv\", RESULTS / \"forecast_normalized.json\",\n                  RESULTS / \"normalized_contract.json\", RESULTS / \"headline.csv\",\n                  RESULTS / \"headline.json\", RESULTS / \"series_coverage.csv\"]:\n            if p.exists(): p.unlink()\n        _write_manifest(run_id, \"FAILED\", started_at_utc=start, kernel=args.kernel,\n                        log_file=str(log_path.relative_to(ROOT)), error=str(exc),\n                        input_snapshot=input_snapshot)\n        return 1\n    _write_manifest(run_id, \"SUCCESS\", started_at_utc=start, kernel=args.kernel,\n                    log_file=str(log_path.relative_to(ROOT)), input_snapshot=input_snapshot,\n                    normalized_result=\"results/oxlon/forecast_normalized.csv\",\n                    normalized_output_sha256=_sha256(RESULTS / \"forecast_normalized.csv\"),\n                    headline_result=\"results/oxlon/headline.csv\",\n                    headline_output_sha256=_sha256(RESULTS / \"headline.csv\"),\n                    series_coverage=\"results/oxlon/series_coverage.csv\",\n                    series_coverage_sha256=_sha256(RESULTS / \"series_coverage.csv\"))\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n";
