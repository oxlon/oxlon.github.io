window.MIIS_V4_DATA=window.MIIS_V4_DATA||{};window.MIIS_V4_DATA["3ec281cbb9c017c6953e30222e2dda421484fbcd30f9149f5ce3e1d532d7b06b"]="Attribute VB_Name = \"HP_priors\"\n' User defined function HPP(): HP filter with priors\n' The filtered value is identical to a prior if the prior is specified\n' If no prior is specified, the result is identical to conventional HP\n' It implements https://mpra.ub.uni-muenchen.de/34202/1/MPRA_paper_34202.pdf\n\n' Input:\n'    -data range (Nx1)\n'    -prior range (Nx1)\n'    -smooth coefficient\n' Output:\n'    -filtered series\n\n' Auxiliary functions:\n'    - t(x): transpose\n'    - m(x,y): matrix multiplication\n'    - sm(x,y): scalar x matrix\n'    - ad(x,y): matrix addition\n'    - inv(x,y): matrix invert\n\n' questions to che@imf.org\n\n\n\nOption Base 1 ' essential. the first element of an array is numbered 1.\n\nFunction HPP(data As Range, prior As Range, lambda)\n    \n    Dim nobs As Long, row As Long, np As Long, npobs As Long, i As Long, j As Long\n    Dim y(), yhp(), Id(), A(), B(), C(), tau()  ' data, HP filtered series, identity matrix, A-B-C-tau matrix as defined in the paper respectively\n    \n    nobs = data.Rows.Count ' length of the data series\n    npobs = prior.Rows.Count ' length of the prior series\n    np = Application.WorksheetFunction.CountA(prior) ' number of priors\n    If npobs <> nobs Then ' throw an error message if the size differ between first two arguments\n        MsgBox (\"Error: Size differ between raw data and prior\")\n        Exit Function\n    End If\n    \n    ReDim y(nobs, 1)\n    ReDim Id(nobs, nobs)\n    ReDim A(nobs, nobs)\n    ReDim C(nobs - 2, nobs)\n    ReDim yhp(nobs, 1)\n    \n    ' convert range to variant\n    For i = 1 To nobs Step 1\n        y(i, 1) = data(i, 1)\n    Next i\n    \n    For row = 1 To nobs - 2 Step 1 ' Generate the C matrix\n        C(row, row) = 1\n        C(row, row + 1) = -2\n        C(row, row + 2) = 1\n    Next row\n    \n\n    Id = Application.WorksheetFunction.Munit(nobs) ' Generate identity matrix\n    A = inv(ad(Id, sm(lambda, m(t(C), C)))) ' A=[I + lambda*C^{T}*C]^{-1}\n    yhp = m(A, y) ' yhp = A*y, the standard HP result\n    \n    If np > 0 Then ' if there are restrictions\n        \n        ReDim B(np, nobs)\n        ReDim tau(np, 1)\n\n        i = 1\n        For j = 1 To nobs Step 1 ' create B and tau matrix\n            If prior(j) <> \"\" Then\n                B(i, j) = 1\n                tau(i, 1) = prior(j)\n                i = i + 1\n            End If\n        Next j\n\n        HPP = ad(yhp, m(m(A, t(B)), m(inv(m(m(B, A), t(B))), ad(tau, sm(-1, m(B, yhp)))))) ' Tr = yhp + A*B^{t}*(B*A*B^{t})^{-1}*(tau-B*yhp)\n    Else\n        HPP = yhp\n    End If\nEnd Function\n\n' Below are the auxiliary functions\n' The function inv() cannot deal with very large matrix.\n' It is an restriction inherited from the excel built in function MInverse().\n' In the future, Gauss-Jordan elimination algorithm can be implemented.\n\nFunction t(x)  'transpose matrix. x must be a matrix\n    Dim i As Long, j As Long, m As Long, n As Long\n    Dim y()\n    m = UBound(x, 1)\n    n = UBound(x, 2)\n    ReDim y(n, m)\n\n    For i = 1 To m Step 1\n        For j = 1 To n Step 1\n        y(j, i) = x(i, j)\n        Next j\n    Next i\n\n    t = y\nEnd Function\n\nFunction m(x, y) ' matrix multiplication. x and y must be matrix\n    Dim i As Long, j As Long, k As Long, xr As Long, xc As Long, yr As Long, yc As Long\n    Dim z()\n\n    xr = UBound(x, 1) ' matrix size row & col\n    xc = UBound(x, 2)\n    yr = UBound(y, 1)\n    yc = UBound(y, 2)\n\n\n    If xc = yr Then ' matrix multiplication\n        ReDim z(xr, yc)\n        For i = 1 To xr Step 1\n            For j = 1 To yc Step 1\n                z(i, j) = 0\n                For k = 1 To xc Step 1\n                    z(i, j) = z(i, j) + x(i, k) * y(k, j)\n                Next k\n            Next j\n        Next i\n\n    Else\n        MsgBox (\"Error: Likely dimension mismatch.\")\n        Exit Function\n    End If\n\n    m = z\nEnd Function\n\nFunction sm(x, y) ' scalar matrix multiplication. x must be a scalar, y must be a matrix\n    Dim i As Long, j As Long, yr As Long, yc As Long\n    Dim z()\n    \n    yr = UBound(y, 1)\n    yc = UBound(y, 2)\n    \n    ReDim z(yr, yc)\n    For i = 1 To yr Step 1\n        For j = 1 To yc Step 1\n        z(i, j) = x * y(i, j)\n        Next j\n    Next i\n    \n    sm = z\nEnd Function\n\nFunction ad(x, y) ' matrix addition. x and y must be matrix\n    Dim i As Long, j As Long, m As Long, n As Long\n    Dim z()\n    m = UBound(x, 1)\n    n = UBound(x, 2)\n    ReDim z(m, n)\n    \n    For i = 1 To m Step 1\n        For j = 1 To n Step 1\n        z(i, j) = x(i, j) + y(i, j)\n        Next j\n    Next i\n    \n    ad = z\nEnd Function\n\n\nFunction inv(x) ' inverse matrix. x must be a matrix\n    If UBound(x, 1) = 1 And UBound(x, 2) = 1 Then ' if x is a 1x1 matrix.\n        Dim y(1, 1)\n        y(1, 1) = 1 / x(1, 1)\n        inv = y\n    Else\n        inv = Application.WorksheetFunction.MInverse(x)\n    End If\n    \n    'Note\n    'What if use \"inv = Application.WorksheetFunction.MInverse(x)\" only?\n    'The output will not be a 1x1 matrix if the input is 1x1 matrix!\n    'It makes the matrix operation functions ad(),sum(),m(),t() complex to write, requiring extra work on dimensionality.\n    'To avoid this, use the If-Else\nEnd Function\n    \n   \n\n";
