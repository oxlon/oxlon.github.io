window.MIIS_V4_DATA=window.MIIS_V4_DATA||{};window.MIIS_V4_DATA["0f194d5f91cefad5844fd50b43525005e13cd71f0fd9121e01b785b1f73d0299"]="#!/usr/bin/env python3\n\"\"\"Publish one governed, provenance-rich OXLON result snapshot.\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport hashlib\nimport os\nfrom datetime import datetime, timezone\nfrom pathlib import Path\n\nimport pandas as pd\n\ntry:\n    from src.governed_inputs import read_validated_overlay\nexcept ImportError:\n    from governed_inputs import read_validated_overlay\n\nBASE = Path(__file__).resolve().parent\nROOT = BASE.parents[1]\nMODEL_OUT = BASE / \"outputs\"\nDATA = BASE / \"data\"\nRESULTS = ROOT / \"results\" / \"oxlon\"\n\nCONTRACT = [\n    \"model_id\", \"scenario_id\", \"vintage_id\", \"series_id\", \"concept\", \"unit\",\n    \"frequency\", \"year\", \"value\", \"status\", \"source_file\", \"source_sheet\",\n    \"source_cell\", \"method\",\n    \"primary_source_file\", \"primary_source_sheet\", \"primary_source_cell\",\n    \"transformation\",\n]\n\n\ndef _sha256(path: Path) -> str:\n    h = hashlib.sha256()\n    with path.open(\"rb\") as fh:\n        for block in iter(lambda: fh.read(1 << 20), b\"\"):\n            h.update(block)\n    return h.hexdigest()\n\n\ndef _status(kind: str, source: str, actual_source: str, series_id: str, year: int) -> str:\n    if source == \"template_sample\":\n        return \"comparison_template\"\n    if source == \"ministry_spec\":\n        return \"scenario_forecast\"\n    if source == \"imf_reference\":\n        return \"benchmark\"\n    if series_id == \"gdp_nonoil_realg_prod\" and kind == \"actual\" and year >= 2016:\n        return \"derived_proxy\"\n    if series_id == \"gdp_nonoil_defl_prod\" and kind == \"actual\" and year >= 2016:\n        return \"derived_identity\"\n    if kind == \"forecast\":\n        return \"model_forecast\"\n    if \"derived_identity\" in actual_source:\n        return \"derived_identity\"\n    if \"official_2025_actuals\" in actual_source:\n        return \"observed_official\"\n    if actual_source:\n        return \"observed_source\"\n    if kind == \"actual\":\n        return \"derived_model_history\"\n    return \"unclassified\"\n\n\ndef _scenario(source: str) -> str:\n    return {\n        \"ours\": \"baseline\",\n        \"template_sample\": \"template_sample\",\n        \"ministry_spec\": \"ministry_spec_comparison\",\n        \"imf_reference\": \"imf_reference\",\n    }.get(source, source)\n\n\n\ndef _output_locator(fr: str) -> tuple[str, str, str]:\n    \"\"\"Return the notebook/block that emitted a row; no raw cell is invented.\"\"\"\n    m = str(fr).split(\"/\")[0].replace(\"FR\", \"\")\n    try:\n        n = int(m)\n    except ValueError:\n        return (\"models/oxlon/outputs/forecast_long.csv\", \"forecast_long\", \"\")\n    nb = BASE / \"notebooks\" / f\"FR{n:02d}.ipynb\"\n    if nb.exists():\n        try:\n            cells = json.loads(nb.read_text(encoding=\"utf-8\"))[\"cells\"]\n            for i, cell in enumerate(cells):\n                if \"outputs.write_forecast_long\" in \"\".join(cell.get(\"source\", [])):\n                    return (f\"models/oxlon/notebooks/FR{n:02d}.ipynb\", f\"FR{n:02d} output\", f\"cell {i}\")\n        except Exception:\n            pass\n    return (f\"models/oxlon/notebooks/FR{n:02d}.ipynb\", f\"FR{n:02d} output\", \"\")\n\n\ndef _primary_locator(series_id: str, year: int, actual_source: str) -> tuple[str, str, str, str]:\n    \"\"\"Resolve only provenance present in the source tag; blank cell means unknown.\"\"\"\n    tag = str(actual_source or \"\")\n    if tag.startswith(\"official_2025_actuals\"):\n        return (\"models/oxlon/data/official_2025_actuals.csv\", \"official_2025_actuals\", f\"series={series_id};year={year}\", \"official 2025 actual replacement\")\n    if tag.startswith(\"derived_identity:\"):\n        bits = tag.split(\":\", 2)\n        fn = bits[1] if len(bits) > 1 else \"\"\n        formula = bits[2] if len(bits) > 2 else \"\"\n        return (f\"models/oxlon/data/{fn}\", \"\", formula, \"data_layer derived identity\")\n    if tag.startswith(\"csv:\"):\n        bits = tag.split(\":\", 2)\n        fn = bits[1] if len(bits) > 1 else \"\"\n        col = bits[2] if len(bits) > 2 else \"\"\n        return (f\"models/oxlon/data/{fn}\", \"\", f\"column={col};year={year}\", \"data_layer CSV observation\")\n    if tag.startswith(\"vereq:\"):\n        ref = tag[len(\"vereq:\"):]\n        sheet, _, row = ref.partition(\"!r\")\n        return (\"models/oxlon/data/cache/vereq_long.csv\", sheet, f\"row={row};year={year}\", \"data_layer statutory workbook extraction\")\n    if tag.startswith(\"wb-avg:\"):\n        ref = tag[len(\"wb-avg:\"):]\n        sheet, _, row = ref.partition(\"!r\")\n        return (\"models/oxlon/data/cache/wb_monthly.csv\", sheet, f\"row={row};year={year}\", \"data_layer annual mean of monthly workbook observations\")\n    if tag.startswith(\"wb:\"):\n        ref = tag[len(\"wb:\"):]\n        sheet, _, row = ref.partition(\"!r\")\n        return (\"models/oxlon/data/cache/wb_annual.csv\", sheet, f\"row={row};year={year}\", \"data_layer workbook observation\")\n    if tag.startswith(\"panel:\") or tag.startswith(\"public:\") or tag.startswith(\"customs:\"):\n        return (\"models/oxlon/data/public_sources_provenance.csv\", \"\", f\"series={series_id};year={year}\", \"data_layer frozen panel observation; locator in provenance file\")\n    return (\"\", \"\", \"\", \"no raw locator available\")\n\n\ndef _row_provenance(r, meta: dict, actual_source: str, actual_map: dict | None = None) -> dict:\n    out_file, out_sheet, out_cell = _output_locator(str(getattr(r, \"fr\", \"\")))\n    primary_file, primary_sheet, primary_cell, transformation = _primary_locator(str(r.series_code), int(r.year), actual_source)\n    source = str(r.source)\n    kind = str(r.kind)\n    if source == \"template_sample\":\n        transformation = \"comparison-only statutory forecast block; not model input\"\n        primary_file = \"models/oxlon/data/8_vereq_original.xlsx\"\n        primary_sheet = str(meta.get(\"statutory_sheet_ref\", \"\"))\n        primary_cell = \"forecast block; exact workbook cell not represented in copied panel\"\n    elif source == \"imf_reference\":\n        transformation = \"quarantined benchmark snapshot; live provenance not validated\"\n        primary_file = \"models/oxlon/data/reference_forecasts.json\"\n        primary_sheet = \"reference_forecasts\"\n        primary_cell = f\"series={r.series_code};year={int(r.year)}\"\n    elif kind == \"forecast\":\n        primary_file = \"\"\n        primary_sheet = \"\"\n        primary_cell = \"\"\n        transformation = \"model estimate from notebook equations and governed assumptions; no single raw cell\"\n    elif str(r.series_code) in {\"gdp_nonoil_realg_prod\", \"gdp_nonoil_defl_prod\"} and int(r.year) >= 2016:\n        # FR2's reclassified production level has no single raw cell.  Resolve the\n        # growth proxy to the actual statutory series for the same year; if the\n        # source registry has no locator, leave it blank rather than inventing a\n        # 2025-only official file reference for earlier years.\n        proxy_src = (actual_map or {}).get((\"nonoil_realg\", int(r.year)), \"\")\n        primary_file, primary_sheet, primary_cell, _ = _primary_locator(\"nonoil_realg\", int(r.year), proxy_src)\n        if str(r.series_code) == \"gdp_nonoil_realg_prod\":\n            transformation = \"statutory non-oil real-growth proxy paired with reclassified FR2 production level; published as proxy\"\n        else:\n            transformation = \"implicit deflator derived from matched FR2 production level and statutory growth proxy; production source has no single raw cell\"\n    return {\n        \"source_file\": out_file, \"source_sheet\": out_sheet, \"source_cell\": out_cell,\n        \"primary_source_file\": primary_file, \"primary_source_sheet\": primary_sheet,\n        \"primary_source_cell\": primary_cell, \"transformation\": transformation,\n    }\n\ndef build(run_id: str) -> pd.DataFrame:\n    src = pd.read_csv(MODEL_OUT / \"forecast_long.csv\")\n    dictionary = pd.read_csv(MODEL_OUT / \"series_dictionary.csv\")\n    actuals_path = DATA / \"cache\" / \"actuals_long.csv\"\n    actuals = pd.read_csv(actuals_path) if actuals_path.exists() else pd.DataFrame()\n    actual_map = {}\n    if not actuals.empty:\n        actual_map = {(str(r.series_code), int(r.year)): str(r.source)\n                      for r in actuals.itertuples()}\n    dmap = dictionary.set_index(\"series_code\").to_dict(\"index\")\n    rows = []\n    for r in src.itertuples(index=False):\n        meta = dmap.get(str(r.series_code), {})\n        source = str(r.source)\n        year = int(r.year)\n        a_source = actual_map.get((str(r.series_code), year), \"\")\n        prov = _row_provenance(r, meta, a_source, actual_map)\n        rows.append({\n            \"model_id\": \"oxlon.corrected.20260917\",\n            \"scenario_id\": _scenario(source),\n            \"vintage_id\": f\"oxlon-run-{run_id}\",\n            \"series_id\": str(r.series_code),\n            \"concept\": str(meta.get(\"name_en\", r.series_name_az)),\n            \"unit\": str(r.unit),\n            \"frequency\": \"annual\",\n            \"year\": year,\n            \"value\": float(r.value),\n            \"status\": _status(str(r.kind), source, a_source, str(r.series_code), year),\n            \"source_file\": prov[\"source_file\"],\n            \"source_sheet\": prov[\"source_sheet\"],\n            \"source_cell\": prov[\"source_cell\"],\n            \"method\": f\"OXLON pipeline; source={source}; kind={r.kind}; {prov['transformation']}\",\n            \"primary_source_file\": prov[\"primary_source_file\"],\n            \"primary_source_sheet\": prov[\"primary_source_sheet\"],\n            \"primary_source_cell\": prov[\"primary_source_cell\"],\n            \"transformation\": prov[\"transformation\"],\n        })\n    out = pd.DataFrame(rows, columns=CONTRACT)\n    if out.duplicated([\"scenario_id\", \"series_id\", \"year\"]).any():\n        raise ValueError(\"duplicate normalized scenario/series/year keys\")\n    return out\n\n\ndef append_official_overlay(out: pd.DataFrame) -> pd.DataFrame:\n    \"\"\"Append separately governed official rows without touching model outputs.\"\"\"\n    # The wrapper archives the canonical file before any DAG stage.  Use that\n    # frozen path for the published rows so normalization cannot diverge from\n    # the drivers consumed by the run or silently repair malformed metadata.\n    path = Path(os.environ.get(\"MIIS_GOVERNED_OVERLAY\", str(ROOT / \"data\" / \"official_overrides.csv\")))\n    overlay = read_validated_overlay(path)\n    if overlay.empty:\n        return out\n    overlay = overlay.reindex(columns=CONTRACT[:14], fill_value=\"\").copy()\n    for col in CONTRACT[14:]:\n        overlay[col] = \"\"\n    overlay[\"transformation\"] = \"validated governed input record\"\n    if overlay.duplicated([\"scenario_id\", \"series_id\", \"year\"]).any():\n        raise ValueError(\"duplicate official overlay scenario/series/year keys\")\n    # The overlay is a distinct scenario and remains outside model-output scope\n    # deletes.  A duplicate baseline key is allowed only because the scenario is\n    # explicit; a duplicate official key is rejected above.\n    return pd.concat([out, overlay], ignore_index=True)\n\n\ndef main() -> int:\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--run-id\", required=True)\n    args = ap.parse_args()\n    RESULTS.mkdir(parents=True, exist_ok=True)\n    out = append_official_overlay(build(args.run_id))\n    dictionary = pd.read_csv(MODEL_OUT / \"series_dictionary.csv\").set_index(\"series_code\").to_dict(\"index\")\n    out.to_csv(RESULTS / \"forecast_normalized.csv\", index=False)\n    out.to_json(RESULTS / \"forecast_normalized.json\", orient=\"records\", force_ascii=False, indent=2)\n    headline_ids = [\"gdp_realg\", \"nonoil_realg\", \"cpi_infl\", \"gdp_nom\"]\n    headline = out[(out[\"scenario_id\"] == \"baseline\") & out[\"series_id\"].isin(headline_ids)].copy()\n    headline = headline.sort_values([\"series_id\", \"year\"])\n    headline.to_csv(RESULTS / \"headline.csv\", index=False)\n    headline.to_json(RESULTS / \"headline.json\", orient=\"records\", force_ascii=False, indent=2)\n    fr_rows = []\n    for series_id, g in out[out[\"scenario_id\"] == \"baseline\"].groupby(\"series_id\"):\n        fr_rows.append({\"series_id\": series_id, \"fr\": str(dictionary.get(series_id, {}).get(\"fr\", \"\")),\n                        \"row_count\": int(len(g)), \"min_year\": int(g[\"year\"].min()),\n                        \"max_year\": int(g[\"year\"].max())})\n    pd.DataFrame(fr_rows).to_csv(RESULTS / \"series_coverage.csv\", index=False)\n    (RESULTS / \"normalized_contract.json\").write_text(json.dumps({\n        \"columns\": CONTRACT,\n        \"required_columns\": [\"model_id\", \"scenario_id\", \"vintage_id\", \"series_id\", \"concept\", \"unit\", \"frequency\", \"year\", \"value\", \"status\", \"source_file\", \"source_sheet\", \"source_cell\", \"method\"],\n        \"provenance_columns\": [\"primary_source_file\", \"primary_source_sheet\", \"primary_source_cell\", \"transformation\"],\n        \"run_id\": args.run_id,\n        \"row_count\": int(len(out)),\n        \"official_overlay_file\": \"data/official_overrides.csv\",\n        \"generated_at_utc\": datetime.now(timezone.utc).isoformat(),\n    }, ensure_ascii=False, indent=2) + \"\\n\", encoding=\"utf-8\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n";
