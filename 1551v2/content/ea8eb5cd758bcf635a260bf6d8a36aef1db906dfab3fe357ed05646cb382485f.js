window.MIIS_V4_DATA=window.MIIS_V4_DATA||{};window.MIIS_V4_DATA["ea8eb5cd758bcf635a260bf6d8a36aef1db906dfab3fe357ed05646cb382485f"]="#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"build_results_json.py — «nəticələrin json fayl formatında yadda saxlanılması».\n\nMİİS §15.5.1-in FR1–FR12 üzrə alt-tapşırıq 5-i nəticələrin İKİ maşın-oxunan formada\nsaxlanılmasını tələb edir: Excel (xlsx) VƏ json. Excel təqdimatını `build_results_xlsx.py`\nyığır; bu skript isə eyni nəticələrin JSON təqdimatını — `neticeler_15_5_1.json` —\nhazırlayır. Hər iki fayl nəticələr qovluğunda, beş CSV faylının yanında dayanır.\n\nQAYDA. Fayldakı HEÇ BİR RƏQƏM əl ilə yazılmır. Yeganə girişlər dashboard müqaviləsinin\nöz fayllarıdır:\n\n    forecast_long.csv        — bütün sıralar, faktiki + proqnoz, 80/50 % zolaqlar, mənbə\n    equations_catalog.csv    — qiymətləndirilmiş tənliklərin kataloqu\n    validation_backtest.csv  — geriyə doğru sınağın nəticələri\n    series_dictionary.csv    — AZ/EN adlar, ölçü vahidi, FR, qiymət bazası\n    assumptions.csv          — VAHİD fərziyyə dəsti\n    assumption_registry.csv  — əvəzləmə reyestri (açar → sinif, sahib qurum)\n\nBÖLMƏLƏR (sabit sıra ilə):\n    meta · proqnoz · tenlikler · geriye_sinaq · sira_lugeti · ferziyyeler ·\n    evezleme_reyestri\n\nTƏKRAR İSTEHSAL. Fayl BAYT-BAYT sabitdir: açarlar əlifba sırası ilə yazılır, sətirlərin\nsırası CSV-lərdəki təbii açar sırası ilə eynidir, faylda heç bir zaman möhürü, icra\nnömrəsi və ya mütləq yol saxlanılmır. Eyni girişlərdən eyni bayt ardıcıllığı alınır.\n\nFORMAT. Sənədin özü JSON obyektidir; hər məlumat bölməsi obyektlər massividir və hər\nsətir AYRICA SƏTİRDƏ yazılır — beləliklə fayl həm proqramla, həm də adi mətn alətləri\n(`grep`, `diff`) ilə oxuna bilir. Boş xana JSON `null`-udur (boş sətir deyil).\nƏdədi sütunlar ədəd, mətn sütunları sətir kimi verilir.\n\nİstifadə:  python3 build_results_json.py\n\"\"\"\nfrom __future__ import annotations\n\nimport csv\nimport json\nimport sys\nfrom pathlib import Path\nfrom typing import Dict, List, Optional, Sequence\n\nBASE = Path(__file__).resolve().parent\nsys.path.insert(0, str(BASE / \"src\"))\nimport config  # noqa: E402\n\nOUT_JSON = Path(config.OUT) / \"neticeler_15_5_1.json\"\n\n# ==========================================================================\n# Bölmə tərifləri — hansı fayl, hansı JSON açarı, hansı sütunlar ədəddir\n# ==========================================================================\nINT_COLS: Dict[str, Sequence[str]] = {\n    \"proqnoz\": (\"year\",),\n    \"tenlikler\": (\"sample_start\", \"sample_end\"),\n    \"geriye_sinaq\": (\"vintage_year\", \"horizon_h\"),\n    \"sira_lugeti\": (),\n    \"ferziyyeler\": (\"year\",),\n    \"evezleme_reyestri\": (),\n}\nFLOAT_COLS: Dict[str, Sequence[str]] = {\n    \"proqnoz\": (\"value\", \"lo80\", \"hi80\", \"lo50\", \"hi50\"),\n    \"tenlikler\": (\"adj_r2\", \"se_regression\"),\n    \"geriye_sinaq\": (\"actual\", \"forecast\", \"error\", \"abs_error\",\n                     \"rmse_h\", \"rw_rmse_h\", \"coverage80\"),\n    \"sira_lugeti\": (),\n    \"ferziyyeler\": (\"value\",),\n    \"evezleme_reyestri\": (),\n}\nSECTION_AZ: Dict[str, str] = {\n    \"proqnoz\": \"Sıraların illik dəyərləri (faktiki + proqnoz, 80/50 % zolaqlar)\",\n    \"tenlikler\": \"Qiymətləndirilmiş tənliklərin kataloqu\",\n    \"geriye_sinaq\": \"Geriyə doğru sınağın (backtest) nəticələri\",\n    \"sira_lugeti\": \"Sıra lüğəti: AZ/EN adlar, vahid, FR, statutar vərəq, qiymət bazası\",\n    \"ferziyyeler\": \"Vahid fərziyyə dəsti — proqnoz üfüqünün bütün ekzogen girişləri\",\n    \"evezleme_reyestri\": (\"Əvəzləmə reyestri — hər fərziyyə açarının sinfi (A: rəsmi/ekzogen \"\n                          \"giriş, B: modul nəticəsi, C: törəmə/qapalı) və sahib qurumu\"),\n}\n\n\ndef _sources() -> Dict[str, Path]:\n    out = Path(config.OUT)\n    return {\n        \"proqnoz\": out / \"forecast_long.csv\",\n        \"tenlikler\": out / \"equations_catalog.csv\",\n        \"geriye_sinaq\": out / \"validation_backtest.csv\",\n        \"sira_lugeti\": out / \"series_dictionary.csv\",\n        \"ferziyyeler\": Path(config.ASSUMPTIONS),\n        \"evezleme_reyestri\": Path(config.ASSUMPTION_REGISTRY),\n    }\n\n\ndef _coerce(section: str, col: str, raw: str) -> Optional[object]:\n    \"\"\"Boş xana → None; ədədi sütun → int/float; qalan hər şey → mətn.\"\"\"\n    s = raw.strip()\n    if s == \"\":\n        return None\n    if col in INT_COLS[section]:\n        try:\n            return int(float(s))\n        except ValueError:\n            return s\n    if col in FLOAT_COLS[section]:\n        try:\n            return float(s)\n        except ValueError:\n            return s\n    return s\n\n\ndef load_section(section: str, path: Path) -> tuple[List[str], List[dict]]:\n    if not path.is_file():\n        raise SystemExit(f\"GİRİŞ FAYLI YOXDUR: {path} — əvvəlcə `python3 run_all.py` işlədin.\")\n    with path.open(newline=\"\", encoding=\"utf-8\") as fh:\n        rdr = csv.reader(fh)\n        header = next(rdr)\n        rows = [{c: _coerce(section, c, v) for c, v in zip(header, rec)} for rec in rdr]\n    return header, rows\n\n\n# ==========================================================================\n# Sabit (deterministik) JSON yazıcısı\n# ==========================================================================\ndef _dump(obj: object) -> str:\n    \"\"\"Açarları əlifba sırası ilə, ASCII-yə çevirmədən, boşluqsuz yazır.\"\"\"\n    return json.dumps(obj, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\"))\n\n\ndef render(meta: dict, sections: Dict[str, List[dict]]) -> str:\n    \"\"\"Üst səviyyə oxunaqlı, hər məlumat sətri isə ayrıca sətirdə — sabit nəticə.\"\"\"\n    parts: List[str] = [\"{\"]\n    parts.append('\"meta\": ' + json.dumps(meta, ensure_ascii=False, sort_keys=True, indent=2) + \",\")\n    keys = list(sections)\n    for i, key in enumerate(keys):\n        rows = sections[key]\n        parts.append(f'\"{key}\": [')\n        for j, row in enumerate(rows):\n            parts.append(_dump(row) + (\",\" if j < len(rows) - 1 else \"\"))\n        parts.append(\"]\" + (\",\" if i < len(keys) - 1 else \"\"))\n    parts.append(\"}\")\n    return \"\\n\".join(parts) + \"\\n\"\n\n\ndef main() -> int:\n    src = _sources()\n    headers: Dict[str, List[str]] = {}\n    sections: Dict[str, List[dict]] = {}\n    for key, path in src.items():\n        headers[key], sections[key] = load_section(key, path)\n\n    meta = {\n        \"senedin_adi\": \"MİİS §15.5.1 — Makroiqtisadi Göstəricilərin Analizi və Proqnozlaşdırılması\",\n        \"hüquqi_esas\": \"Nazirlər Kabinetinin 24.05.2004 tarixli 75 nömrəli qərarı, bənd 2.4.1\",\n        \"son_faktiki_il\": int(config.LAST_ACTUAL),\n        \"proqnoz_illeri\": [int(y) for y in config.FY],\n        \"kodlasdirma\": \"UTF-8\",\n        \"bos_xana\": \"null\",\n        \"qeyd\": (\"Bu fayl `neticeler_15_5_1.xlsx` iş kitabı ilə EYNİ nəticələrin maşın-oxunan \"\n                 \"təqdimatıdır və beş CSV faylından proqram vasitəsilə yığılır; heç bir rəqəm \"\n                 \"əl ilə yazılmır. Fayl bayt-bayt sabitdir (zaman möhürü saxlanılmır).\"),\n        \"bolmeler\": {\n            key: {\n                \"izah\": SECTION_AZ[key],\n                \"menbe_fayl\": src[key].name,\n                \"setir_sayi\": len(sections[key]),\n                \"sutunlar\": headers[key],\n            }\n            for key in src\n        },\n    }\n    OUT_JSON.parent.mkdir(parents=True, exist_ok=True)\n    text = render(meta, sections)\n    OUT_JSON.write_text(text, encoding=\"utf-8\")\n\n    print(f\"JSON yazıldı: {OUT_JSON.name}  ({OUT_JSON.stat().st_size / 1024 / 1024:.2f} MB)\")\n    for key in src:\n        print(f\"   {key:<14} {len(sections[key]):>6} sətir   ← {src[key].name}\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n";
