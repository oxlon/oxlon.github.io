window.MIIS_V4_DATA=window.MIIS_V4_DATA||{};window.MIIS_V4_DATA["812a676e3ad745013684dc084f913edb9414ef6ea9dfceb8c5f81448b4924c45"]="# Ministry macroeconometric source engine\n\nThis model evaluates the complete supplied eleven-workbook network: 109 worksheets and 125,253 original formulas, including original company/product/mode inputs, full-precision parameter cells, year-specific judgement, accounting identities and reporting links. It does not call the previous FR13 catalogue model or replace original rules with independently estimated equations.\n\nThe `sources/ministry_macro` snapshots are immutable originals. Every run verifies workbook and full-network extraction hashes. `build_source.py` records the preserved bytes and independently inventoried cells/shared-formula expansion. `engine.py` evaluates original formulas from that inventory, following local cross-workbook links. It never substitutes a saved cache for a failed calculation. Missing workbooks, source errors, unsupported syntax and cycles remain explicit errors.\n\nUse the bundled Python runtime, which provides openpyxl and lxml. From this folder:\n\n```text\npython run.py --scenario baseline\npython run.py --scenario acg_volume_plus10 --overrides scenarios/acg_volume_plus10.json\npython summarize.py\n```\n\nThe baseline and oil-plan sensitivity outputs are in `results/ministry_macro`. CSV and JSON use the shared project long-form schema, with original workbook/sheet/cell lineage. Original saved references have `status=cached_reference`; successfully evaluated source formulas have `status=run_calculated` and method `portable_original_workbook_formula`. Preserved direct literals have `status=source_input`, explicit edited inputs `status=scenario_input`; only source formulas are labelled calculated. `source_value_type`, `data_status` and `reporting_role` identify literal/formula/override, original year status and statutory versus canonical source control. Shared-formula followers are classified using the expanded network, even when their local formula text is null. Formula errors have `status=run_error`, null value and diagnostic detail. Method also preserves the original historical/2025-expected/2026–2030-forecast boundary; `run_calculated` does not mean observed actual.\n\nThe demonstration changes only original 2026 ACG production `MOE OIL.xlsx!BP!AK6` by +10%. Other independently entered production/export/company plans remain unchanged, as they do under that source-cell edit. It is a sensitivity test, not an official forecast or a calibrated probabilistic shock. Cell edits are validated before execution: existing hardcoded numeric forecast inputs only; headers, formulas and unidentified historical columns are protected; metadata/date/unit checks and previous value are recorded. The override file stays separate and is reapplied to every run, so generated baseline writers cannot overwrite it. An official updated-data scenario still needs an approved concept-to-cell/vintage bridge; this demonstration does not invent one.\n\nNative workflow support is explicit:\n\n```text\npython prepare_native.py --destination /absolute/disposable/empty/folder\npython prepare_native.py --destination /absolute/disposable/shock/folder --overrides scenarios/acg_volume_plus10.json\npython export_native.py --working /absolute/native/saved/folder --attestation /absolute/engine_record.json --scenario native_baseline --output /absolute/result/folder\npython compare_native.py --portable ../../results/ministry_macro/baseline --native /absolute/result/folder --output /absolute/comparison/folder\n```\n\nPreparation copies originals, remaps external relationships to local working paths, requests full automatic recalc, preserves XML namespaces and applies permitted input edits. It does **not** calculate. A separate accepted native controller must load all books, update existing local links, fully recalculate and save all. Export requires the controller's engine/version/run ID/actions plus matching hashes for all eleven saved files. No missing workbook is fabricated. `native_baseline_working_v2` is the valid prepared set; the superseded earlier preparation is retained outside the delivery under scratch work and must not be used.\n\nBaseline evidence is strong source-cache parity, not native-engine acceptance: all 121,011 numeric cached formula results match at relative tolerance 1e−9 / absolute 1e−8; 1,331 text results and 2,564 saved error codes match. 347 remaining differences are reported: 340 missing-book effects, five stale error-code discrepancies in industry diagnostics, and two cached error cells that calculate numerically under the declared source decimal-comma text profile. No unsupported formulas or cycles occur in these runs. Observed native baseline and controlled ACG calculations now match all comparable numeric formula values; full-force/link-refresh and locale/missing-data acceptance remain separate gates.\n\nThe machine-readable FR reporting crosswalk covers FR1–FR12 using original statutory sheets and direct source controls, plus fiscal B18. It does not assert complete acceptance of every supplemental TT atom. It preserves original gaps: 98 mapped forecast error rows, mostly broken product references, and unmapped unit/owner details. See `reports/ministry_macro` for coverage, full formula errors, input registry, parity and shock transmission.\n\nVerified latest-input bridge: `data/governance/validate_latest_input_bridge.py` checks only approved hardcoded ACG physical-production and world-crude-price source mappings. Earlier candidate direct GDP/CPI/FX cells are rejected because they are shared formulas. Null local formula text is not evidence of a hardcoded input. Full latest official history needs approved upstream branch/tax/price and company mappings, not overwriting calculated outputs.\n\nPublication is transactional: a run writes only to hidden staging, writes `status=SUCCESS` last, and publishes the completed directory. Previous output directories are archived; failed runs keep the old successful publication intact with separate failure diagnostics. Custom judge output paths use the same transaction. A baseline cannot contain overrides, override scenario IDs must match, booleans are rejected as numeric inputs, and publication cannot target preserved source trees.\n\nAn independent native Calculate Now/save attempt is documented in `reports/ministry_macro/native_calculate_now_attempt`: 125,253 source formulas compared, 121,009 numeric cache matches, no numeric differences, and 349 explicit nonmatching cell results (340 missing-book effects,5 industry error-code differences,4 source-locale differences). This does not attest full-force calculation or every external-link refresh. `diagnose_native_attempt.py` performs read-only stable-hash extraction; it does not automate Excel or claim native acceptance.\n\nControlled native ACG evidence is frozen in `reports/ministry_macro/native_acg_volume_plus10_attempt`: verified BP!AK6=132.13365, all 5,587 supported changed numeric cells transmitted, and zero numeric port/native differences over125,253 formula addresses. Native extra 56 changed SOCIAL comparisons retain unavailable ert.xlsx caches; these are explicitly excluded from supported dependency evidence. Exact files for both native cases are frozen under their respective `saved_workbooks` folders. The mutable working_v2 folder currently contains the shock.\n\nVerified repeated statutory year-header rows (2.4.1.5.-7. rows9/29/53;2.4.1.13.-14. row18) are excluded from economic output mapping. Full original formulas remain calculated. All three publication reruns remove71header rows only; retained values and formula-result files are unchanged. See `HEADER_MAPPING_CORRECTION.json`.\n\nThe source category-heading row2.4.1.2./186 is also excluded: it links to blank SNA category cells and yields zero without defining a metric. Actual measures start187/188/189. Current10,448 economic rows have no unresolved unit metadata. Current ACG/world-price class pairs were each physically recalculated twice under the corrected runner, with immutable public run1+run2 snapshots; prior9b8927 evidence is separately labelled historical.\n";
